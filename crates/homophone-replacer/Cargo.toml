[package]
name = "homophone-replacer"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
